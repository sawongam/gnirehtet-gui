# Sidecar binaries (not committed)

Drop the upstream `gnirehtet` executable here with a Rust target-triple suffix.
See `apps/desktop/docs/SIDECAR.md`.

This directory is intentionally empty in git (except this README / `.gitkeep`).
